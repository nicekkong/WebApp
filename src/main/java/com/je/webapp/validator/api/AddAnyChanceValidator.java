package com.je.webapp.validator.api;

import com.je.webapp.form.api.UserApiForm;

public class AddAnyChanceValidator {

	public String validate(UserApiForm apiForm) {
		
/*
		String errormsg = null; //파라미터값에 이상이 있을 경우 관련 에러메시지

		if(StringUtil.changeNullToEmpty(apiForm.getMbrId()).length() == 0) {
			errormsg = "MBR_ID 파라미터가 없습니다.";
			return errormsg;
		}
		
		if(StringUtil.changeNullToEmpty(apiForm.getServiceCd()).length() == 0) {
			errormsg = "서비스코드 파라미터가 없습니다.";
			return errormsg;
		}
				
		return errormsg;

*/
        return null;
		
	}

}